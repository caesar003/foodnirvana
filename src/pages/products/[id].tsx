import Layout from '@components/Layout'
import React from 'react'

export default function Product({params}: {params: {id: string}}) {
  return (
    <Layout>
        <div>products single</div>
    </Layout>
  )
}
